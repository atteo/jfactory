gerrit:
    build: gerrit/
    expose:
        - "8080"
    environment:
        "PROXY_URL":
        "LDAP_URL":
        "LDAP_ACCOUNT_BASE":
        "LDAP_GROUP_BASE":
        "SMTP_SERVER":
        "SMTP_SERVER_PORT":
        "SMTP_FROM":
    volumes:
        - ./data/gerrit-volume:/home/gerrit/review_site
proxy:
    image: nginx
    ports:
        - "80:80"
    links:
        - gerrit
    volumes:
        - ./proxy/nginx.conf:/etc/nginx/conf.d/default.conf:ro

